# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Capacitor native projects (Epic 10). `android/` is still a regenerable spike
# (`npx cap add android` from capacitor.config.ts + dist/) — not tracked yet.
# `ios/` graduated: it now carries real customizations (app icon, splash) and is
# tracked below. Its own ios/.gitignore excludes the synced web bundle (App/App/public),
# Pods, DerivedData, and generated config files.
/android

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
